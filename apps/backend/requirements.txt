# FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Pydantic
pydantic==2.10.5
pydantic-settings==2.1.0

# Environment
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.27.0
aiosqlite==0.19.0  

# Linting & Type checking
ruff==0.1.14
mypy==1.8.0

# Scheduling
APScheduler==3.10.4

# PDF & Report Generation
reportlab==4.0.9

# Authentication
google-auth==2.26.2
supabase

# Payment Gateway
midtransclient==1.4.2

# Machine Learning (runtime)
# Stunting risk inference uses pure-python sigmoid + dot product on hardcoded
# Logistic Regression coefficients (see app/services/stunting_risk_service.py).
# Zero runtime ML deps — kept lean for serverless/Railway memory limits.
#
# Retraining (offline only, not deployed) requires:
#   pip install numpy==1.26.4 scikit-learn==1.4.2 pandas==2.2.2
#   python -m scripts.train_stunting_model
# Output JSON gets committed to repo; service auto-loads it on next deploy.
